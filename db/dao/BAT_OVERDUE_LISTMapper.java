package com.bea.dao;

import com.bea.olp.BAT_OVERDUE_LIST;

public interface BAT_OVERDUE_LISTMapper {
    int insert(BAT_OVERDUE_LIST record);

    int insertSelective(BAT_OVERDUE_LIST record);
}